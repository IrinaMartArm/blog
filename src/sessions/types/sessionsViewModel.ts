export type SessionsViewModel = {
  ip: string;
  title: string;
  lastActiveDate: string;
  deviceId: string;
};
export type SessionsDbModel = {
  ip: string;
  title: string;
  lastActiveDate: string;
  deviceId: string;
};
